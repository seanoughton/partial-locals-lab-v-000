Classroom Info
<p>
<%= @classroom.course_name %>
<%= @classroom.semester %>
</p>

<h1>Students</h1>
<!--
<%= render partial: "students/student", collection: @classroom.students %>
-->

<%@classroom.students.each do |student| %>
  <%binding.pry%>
  <%= render partial: "students/student", locals: {classroom_student: student}%>
<%end%>




We now want to call out the oldest student in the class:
<%= render 'students/student' %>
